/*--------------------------------------------------
DELETE CONSERVATION
----------------------------------------------------*/
// placeholder values:
// [conservator, article_id]
export const deleteConservation =
  "DELETE FROM digging.conservations WHERE `conservator` = ? AND `article_id` = ?";
