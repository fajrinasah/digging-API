/*--------------------------------------------------
(SELECT) COUNT ARTICLE'S CONSERVATORS
----------------------------------------------------*/

// placeholder values:
// [article_id]
export const countArticleConservators =
  "SELECT * FROM total_conservators WHERE article_id = ?";
