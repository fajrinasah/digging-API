/*--------------------------------------------------
(SELECT) COUNT ARTICLE'S CONSERVATORS
----------------------------------------------------*/

// placeholder values:
// [article_id]
export const countArticleConservators =
  "SELECT * FROM vw_most_conserved WHERE article_id = ?";
